---
features:
  - |
    Added a new method `set_optimizer` to the `QuantumGenerator` class, whhich allows
    for the optimizer to be changed while keeping thesame generator. Previously,
    we needed to create a new generator if we wanted to change the optimizer.